import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF11659D);
  static const primaryDark = Color(0xFF0A466E);
  static const veryLightPrimary = Color(0xFFE5EDF1);
  static const primaryLight = Color.fromARGB(255, 24, 135, 209);
  static const black = Color(0xFF101010);
  static const white = Colors.white;
}
